spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hims
    username: postgres
    password: 200109803121
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: localhost
    port: 1025
    username: dev
    password: dev
    properties:
      mail:
        smtp:
          trust: "*"
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:9090/realms/hospital-inventory"
          jwk-set-uri: "http://localhost:9090/realms/hospital-inventory/protocol/openid-connect/certs"
keycloak:
  auth-server-url: http://localhost:9090
  realm: hospital-inventory
  service-account:
    client-id: hims
    client-secret: pSTjpHyDbVvBHTeCxfY2B7BgZy8Y3HAf

application:
server:
  port: 8088
